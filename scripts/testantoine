#!/bin/bash

source "$CUSTOM_DIR/includes/functions.sh"

main() {
    echo "TEst"
    echo "CUSTOM_DIR = $CUSTOM_DIR"

    # echo -e '\e[93m=============================================\033[0m'
    # echo "Choisissez l'instance à mettre à jour parmi les domaines disponibles : "
    # listSite=($(getcvsites | sort))
    # select instance in "${listSite[@]}"; do break; done
    # [ -z "$instance" ] && echo -e "${RED}[ ERREUR ]${NC} Instance invalide" && exit 1


    # # Tester recalculer_serialisation
    # recalculer_serialisation "/var/www/vhosts/vps1.gestad.net/20260227091059_vps1_gestad_net.sql"
    # local my_common="${CUSTOM_DIR%/scripts_server}"
    # echo "my_common = $my_common"

    # local mysql_source_database="$my_common/TMP/civicrm_setting"
    # echo "mysql_source_database = $mysql_source_database"

    # recalculer_serialisation "$mysql_source_database.sql"

    #recalculer_serialisation "/var/www/vhosts/test1.all-in-appli.com/TEST/civicrm_setting.sql" # ok
    # recalculer_serialisation  "/var/www/vhosts/test1.all-in-appli.com/TEST/de_civicrm_relationship_cache_TO_civicrm_setting.sql"
    
    # echo "backup_part_1"
    # recalculer_serialisation  "/var/www/vhosts/test1.all-in-appli.com/TEST/backup_part_1.sql" -v
    # echo "backup_part_2"
    # recalculer_serialisation  "/var/www/vhosts/test1.all-in-appli.com/TEST/backup_part_2.sql" -v
    # echo "backup_part_3"
    # recalculer_serialisation  "/var/www/vhosts/test1.all-in-appli.com/TEST/backup_part_3.sql" -v
    # echo "backup_part_4"
    # recalculer_serialisation  "/var/www/vhosts/test1.all-in-appli.com/TEST/backup_part_4.sql" -v
    # echo "backup_part_5"
    # recalculer_serialisation  "/var/www/vhosts/test1.all-in-appli.com/TEST/backup_part_5.sql" -v
    # echo "backup_part_6"
    # recalculer_serialisation  "/var/www/vhosts/test1.all-in-appli.com/TEST/backup_part_6.sql" -v
    # echo "backup_part_7"
    # recalculer_serialisation  "/var/www/vhosts/test1.all-in-appli.com/TEST/backup_part_7.sql" -v
    # echo "backup_part_8"
    # recalculer_serialisation  "/var/www/vhosts/test1.all-in-appli.com/TEST/backup_part_8.sql" -v
    # echo "backup_part_9"
    # recalculer_serialisation  "/var/www/vhosts/test1.all-in-appli.com/TEST/backup_part_9.sql" -v
    # echo "backup_part_10"
    # recalculer_serialisation  "/var/www/vhosts/test1.all-in-appli.com/TEST/backup_part_10.sql" -v

    # echo "civicrm_setting_reseau-oudinot"
    # recalculer_serialisation  "/var/www/vhosts/test1.all-in-appli.com/TEST/civicrm_setting_reseau-oudinot.sql" -v
    # echo "civicrm_setting_test1"
    # recalculer_serialisation  "/var/www/vhosts/test1.all-in-appli.com/TEST/civicrm_setting_test1.sql" -v

    # # Tester sur une ligne isolée
    # echo 's:462:\"https://syntec.civicrm.makoa.net/civicrm/contact/view/participant?id=67886&amp;cid=35684\";' | python3 -c "
    # import re, sys

    # content = sys.stdin.read()

    # def fix_length_escaped(match):
    #     str_value = match.group(1)
    #     correct_length = len(str_value.encode('utf-8'))
    #     print(f'DEBUG: longueur calculée = {correct_length}', file=sys.stderr)
    #     print(f'DEBUG: longueur réelle chaîne = {len(str_value)}', file=sys.stderr)
    #     return 's:' + str(correct_length) + ':\\\\\"' + str_value + '\\\\\";'

    # pattern_escaped = re.compile(r's:\d+:\\\\\"([^\\\\\"]*?)\\\\\";', re.DOTALL)
    # result = pattern_escaped.sub(fix_length_escaped, content)
    # print(result)
    # "
}

main